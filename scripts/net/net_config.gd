class_name NetConfig
## 联机配置（static 跨场景保留）：由标题画面联机入口填写。

static var enabled := false
static var server_ip := "127.0.0.1"
static var server_port := 24565
static var player_name := ""  # 自定义牛名（空 = 随机荒诞名）
static var room_id := "1"     # 房间号：同号的人在同一家草原（默认 1）
